---
import LabNote from '../../../layouts/LabNote.astro';
import { labNotePaths, type LabNote as Note } from '../../../lib/lab-notes';
export const getStaticPaths = () => labNotePaths('de');
interface Props { note: Note; }
const { note } = Astro.props;
---
<LabNote note={note} />
